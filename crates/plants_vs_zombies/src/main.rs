use std::error::Error;

// use win32_utils::process::inject_dll;
// use windows::core::w;

fn main() -> Result<(), Box<dyn Error>> {
    // unsafe {
    // inject_dll(w!("植物大战僵尸中文版"), w!("game_dll.dll"))?;
    // };

    Ok(())
}
